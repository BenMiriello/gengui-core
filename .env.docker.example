# Docker Stack Environment Variables
# Copy this to .env when using docker-compose
#
# These variables are read by docker-compose.yml and passed to containers
# Sensitive values should be set in your actual .env file (not committed)

# S3 Storage (AWS S3) - REQUIRED
AWS_ACCESS_KEY_ID=your-aws-key
AWS_SECRET_ACCESS_KEY=your-aws-secret
AWS_REGION=us-east-2
S3_BUCKET=gengui-media

# Gemini API - REQUIRED for text analysis and prompt augmentation
GEMINI_API_KEY=your-gemini-api-key

# RunPod Serverless (optional - for GPU workers)
ENABLE_RUNPOD=false
# RUNPOD_API_KEY=RP-your-api-key
# RUNPOD_ENDPOINT_ID=your-endpoint-id
